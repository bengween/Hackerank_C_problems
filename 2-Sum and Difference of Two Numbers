#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main()
{
	  int x,y;
    float w,v;
    
    scanf("%d %d", &x, &y);
    scanf("%f %f", &w, &v);
    
    printf("%d %d\n", x+y,x-y);
    printf("%.1f %.1f", w+v,w-v);
    
    return 0;
}
